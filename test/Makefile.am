TEST_LOG_DRIVER = env PATH="${PATH}:$(abs_top_srcdir)/src" AM_TAP_AWK='$(AWK)' $(SHELL) $(top_srcdir)/build-aux/tap-driver.sh
TEST_LOG_DRIVER_FLAGS=--color-tests $(colour_tests) --no-merge 
TEST_ENVIRONMENT = env PATH="$(abs_top_srcdir):$PATH"
TESTS = diskdevil.test
EXTRA_DIST=diskdevil.test
